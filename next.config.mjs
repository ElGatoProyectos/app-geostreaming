/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
/* const nextConfig = {
  async rewrites() {
    return [
      {
        source: "/api/backend/:path",
        destination: "http://161.132.37.105:4000/:path",
      },
    ];
  },
};

export default nextConfig; */
